import { Header } from '../header';
import { Map } from '../map';
import './app.css';

export const App = () => {
  return (
    <div className="App">
      <Header />
      <Map />
    </div>
  );
};
